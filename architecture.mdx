---
title: Architecture
description: How the SDK, Gennoctua backend, and HP AI fit together.
---

## System Overview

The SDK in the browser handles face/pose, selection, jobs, caching, and rate limits. All HP traffic goes through an auth proxy (Gennoctua or your backend).

```mermaid
flowchart LR
  subgraph browser["Browser — brand site"]
    SDK["@gennoctua/personalize-core"]
  end
  SDK -->|HTTPS| proxy["Auth proxy"]
  proxy -->|HTTPS + HP credentials| hp["HP AI"]
```

The auth proxy is **either** Gennoctua (`token.gennoctua.com`, frontend-only) **or** your own backend (full-stack). The SDK loads **face-api.js** and **MediaPipe**, runs **selection**, **submit + SSE status**, **IndexedDB** caching, and **rate limits**.

---

## Two Auth Paths

### Path A — Frontend-Only (via Gennoctua)

Used by brands with no backend (Shopify, Webflow, plain HTML).

1. Brand receives `gn_pk_...` key from Gennoctua
2. SDK calls `POST /auth/token` with `publicKey`
3. Gennoctua validates key and returns a 1hr JWT
4. SDK uses the JWT for `/api/tryon/submit` and `/api/tryon/status`
5. Gennoctua looks up the brand's HP API key and proxies to HP

```mermaid
sequenceDiagram
  participant B as Browser SDK
  participant G as Gennoctua
  participant H as HP
  B->>G: POST /auth/token publicKey
  Note over G: Validate brand key
  G-->>B: JWT expiresIn 3600
  B->>G: POST /api/tryon/submit Bearer JWT
  Note over G: Validate JWT, lookup hp_api_key
  G->>H: Forward multipart X-API-Key
  B->>G: GET /api/tryon/status jobId SSE
  Note over G: Validate JWT, lookup hp_api_key
  G-->>B: SSE stream from HP
```

### Path B — Full-Stack (brand's own backend)

Used by brands that have their own Node.js, Next.js, Express, etc. backend.

1. Brand stores the HP API key in `.env` (never in the browser)
2. Brand exposes a token endpoint (e.g. `POST /auth/tryon-token`)
3. Brand exposes try-on proxy routes (`POST` + `GET /api/tryon/...`)
4. SDK calls the brand backend with the JWT
5. Brand backend forwards to HP

```mermaid
sequenceDiagram
  participant B as Browser SDK
  participant Y as Brand backend
  participant H as HP
  B->>Y: POST /auth/tryon-token
  Note over Y: Issue JWT with JWT_SECRET
  Y-->>B: JWT expiresIn 3600
  B->>Y: POST /api/tryon/submit Bearer JWT
  Note over Y: Validate JWT, use HP_API_KEY
  Y->>H: Forward to HP
  B->>Y: GET /api/tryon/status jobId SSE
  Y-->>B: SSE stream from HP
```

---

## What Runs Where

| Component | Where it runs | What it does |
|---|---|---|
| Face detection | **Browser** (WebAssembly) | Detects faces in uploaded photos |
| Pose scoring | **Browser** (WebAssembly) | Scores body poses for best photo selection |
| Photo selection | **Browser** | Picks best male/female/child profile photo |
| Token generation | **Backend** (Gennoctua or brand) | Issues short-lived JWTs |
| Job submission | **Backend** (proxy) | Forwards multipart form to HP |
| SSE streaming | **Backend** (proxy) | Streams job status back to browser |
| Result caching | **Browser** (IndexedDB) | Caches try-on results for 7 days |
| AI generation | **HP** | Generates the try-on image |

---

## Data Flow: Full Try-On Lifecycle

```mermaid
flowchart TD
  subgraph init["1. sdk.init"]
    i1["Load face-api.js + MediaPipe from CDN first load ~5 to 10s"]
    i2["Fetch JWT from auth endpoint"]
    i1 --> i2
  end
  subgraph ingest["2. sdk.ingestImages"]
    g1["Filter images max 80"]
    g2["Face detection per photo"]
    g3["Gender + age classification"]
    g4["Pose scoring for body shots"]
    g5["Select best profiles"]
    g6["Return SelectionSummary"]
    g1 --> g2 --> g3 --> g4 --> g5 --> g6
  end
  subgraph pers["3. sdk.personalize"]
    p1["IndexedDB result cache hit short circuit"]
    p2["Restore in-flight job if configured"]
    p3["Client rate limit check"]
    p4["POST submit multipart job_id"]
    p5["GET status jobId SSE until terminal state"]
    p6["Cache result TTL default 7 days"]
    p7["Return imageUrl cacheHit jobId"]
    p1 --> p2 --> p3 --> p4 --> p5 --> p6 --> p7
  end
  init --> ingest
  ingest --> pers
```

---

## HP API

HP is the AI backend that generates the try-on images. Gennoctua manages the relationship with HP — brands never interact with it directly.

| Endpoint | Method | Purpose |
|---|---|---|
| `/api/tryon/submit` | POST | Submit a try-on job |
| `/api/tryon/status/:jobId` | GET (SSE) | Stream job status |
| `/api/eyewear/submit` | POST | Eyewear category |
| `/api/eyewear/status/:jobId` | GET (SSE) | Eyewear status |
| `/api/footwear/submit` | POST | Footwear category |
| `/api/makeup/submit` | POST | Makeup category |
| `/api/accessories/submit` | POST | Accessories category |

The SDK automatically routes to the correct endpoint based on `productType`.

---

## Security Model

| Threat | Mitigation |
|---|---|
| HP API key exposed in browser | Key lives on backend only, never sent to browser |
| Stolen JWT used to make requests | JWTs expire in 1hr, rate limiting caps abuse |
| Brand key leaked | Only gets JWTs, cannot directly call HP |
| Excessive requests (abuse) | Client-side + server-side rate limiting |
| Expired token | SDK auto-refreshes in PublicKey mode, `getToken` called again in proxy mode |
